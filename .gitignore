.DS_Store
Thumbs.db
node_modules/
.vscode/
.idea/
*.log

# Admin tool now published so it can be used from a phone. The HTML
# itself contains no secrets \u2014 the fine-grained PAT lives only in the
# user's browser localStorage on each device.
# admin.html

# Raw route source files \u2014 keep local, don't ship. The simplified
# data/route.geojson is what the app uses at runtime.
data/northern-route.json
data/route-*.backup.geojson

# Raw OG image source (kept local for re-cropping). The cropped 1200x630
# og-image.jpg is what ships.
og-preview.png

# Local-only reorganization archive (deduplicated Sarah photos etc.)
photos/_archive/

# Raw source GPX for Michael's Camino Portugués — stitched output is
# route.geojson which is what the app uses at runtime.
data/journeys/michael-portugues-2026/*.gpx
